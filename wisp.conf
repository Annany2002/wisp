# upstream backend {
#     method round_robin;
#     server 127.0.0.1:3001;
#     server 127.0.0.1:3002;
#
#     # Active HTTP health check. Probes /healthz every 10s with a
#     # 3s per-probe timeout; backend is healthy when it returns 200.
#     # Omit the directive (or set path="") for TCP-dial probing.
#     health_check path=/healthz interval=10s timeout=3s expect=200;
# }
#
# server {
#     listen 8080;
#     server_name wisp;

#     # wisp_test_suite contains static files that need be served
#     location / {
#         root ./wisp_test_site;
#
#         # add_header appends Name: Value to every response sent from
#         # this location. Use double quotes for values that contain
#         # semicolons or spaces (e.g. CSP).
#         add_header X-Frame-Options DENY;
#         add_header Strict-Transport-Security "max-age=31536000; includeSubDomains";
#     }

#     # proxy is set to redirect to server running on port 3000
#     location /api/ {
#         proxy_pass http://127.0.0.1:3000;
#
#         # Override forwarded headers. Supported vars:
#         #   $host, $remote_addr, $scheme, $proxy_add_x_forwarded_for
#         proxy_set_header Host              $host;
#         proxy_set_header X-Real-IP         $remote_addr;
#         proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # WebSocket upgrades are detected automatically (Connection: Upgrade
#     # + Upgrade: websocket) and proxied as a raw bidirectional byte stream.
#     # No extra directive needed — point proxy_pass at any WS backend.
#     location /ws/ {
#         proxy_pass http://127.0.0.1:4000;
#     }
# }

server {
    listen 8443;
    server_name localhost;

    # path to ssl certificate
    ssl_certificate     ./cert.pem;

    # path to ssl certificate key
    ssl_certificate_key ./key.pem;

    location / {
        root ./wisp_test_site;
    }
}